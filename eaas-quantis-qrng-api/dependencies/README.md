Place the `quantis-libraries/` directory here before running `build.sh` or
building the container. Place `quantis-drivers/` here only when the build script
should install the host PCIe driver.

The locations can be overridden for host builds with `QUANTIS_LIBRARIES_DIR`
and `QUANTIS_DRIVERS_DIR`.
